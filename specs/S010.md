# S010: Fetch prune option

## Title
Fetch prune option

## Spec Summary
Add a `prune` argument to the git-control `fetch` tool and helper script so callers can run `git fetch --prune` when needed, plus documentation updates.

## Spec Goal
Allow pruning stale remote-tracking branches through MCP without direct git commands.

## DoD checklist
- [x] Add a `prune` argument to the `fetch` tool
- [x] Update the helper script to pass `prune`
- [x] Update git-control docs and test plan to include `prune`
