# S013: run_git tool

## Title
run_git tool

## Spec Summary
Add a `run_git` tool to execute allowlisted git subcommands with arguments, plus documentation updates.

## Spec Goal
Provide a single generic tool to run common git commands without adding a bespoke tool for each subcommand.

## DoD checklist
- [x] Implement `run_git` tool with an allowlist of common subcommands
- [x] Document destructive warning and allowed subcommands
- [x] Add helper script and update test plan
