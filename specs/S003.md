# S003: MCP server test example

## Title
MCP server test example

## Spec Summary
Provide a working example client that connects over stdio and calls the server tools for a real response.

## Spec Goal
Make it easy to validate the server end-to-end during learning and iteration.

## DoD checklist
- [x] Include a minimal Python client example
- [x] Example calls both `get_alerts` and `get_forecast`
- [x] Include run instructions from project root
