# TC010: Push branch pushes to remote

## Title
Push branch pushes to remote

## Spec Summary
Validate `push_branch` pushes a local branch to the configured remote.

## Spec Goal
Ensure safe, consistent pushes with upstream tracking.

## DoD checklist
- [ ] Remote contains the pushed branch ref after `push_branch`
