# S019: Daily log rotation

## Title
Daily log rotation

## Spec Summary
Add daily log rotation with a 5-file retention policy for the git-control server logs.

## Spec Goal
Keep log storage bounded while preserving recent history for debugging.

## DoD checklist
- [x] Replace the file handler with a timed rotating handler in `src/git_control/server.py`
- [x] Retain 5 daily log files in addition to the current log
- [x] Document log rotation behavior in `docs/git-control.md`
- [x] Update `specs/plan.md`
- [x] Add a log-rotation validation note in `docs/git-control.md`
