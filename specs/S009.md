# S009: Git fetch tool

## Title
Git fetch tool

## Spec Summary
Add a git-control MCP tool and helper script for running `git fetch`, plus documentation updates to cover the new tool.

## Spec Goal
Allow fetching remote updates through MCP without direct git commands.

## DoD checklist
- [x] Add a `fetch` tool wired through the MCP server
- [x] Provide a helper script for the new tool
- [x] Update git-control documentation and test plan to include `fetch`
