# S015: Align script defaults with git behavior

## Title
Align script defaults with git behavior

## Spec Summary
Update git-control root scripts so their default arguments match git equivalents for staging and committing.

## Spec Goal
Make the root scripts behave predictably when called without explicit file arguments, matching git defaults and emitting clear errors when required inputs are missing.

## DoD checklist
- [x] Update git_control_stage_files to default to '.'
- [x] Require explicit file lists for git_control_stage_deletions and git_control_commit_changes
- [x] Update script documentation and examples
- [x] Add or adjust tests if needed
- [x] Update specs/plan.md to include S015
