# S002: Codex CLI integration guide

## Title
Codex CLI integration guide

## Spec Summary
Document how to register the MCP server in Codex CLI using `~/.codex/config.toml` and verify it shows as active.

## Spec Goal
Enable quick local use of the MCP server inside Codex with a repeatable setup.

## DoD checklist
- [ ] Provide `config.toml` example with `command` and `args`
- [ ] Include CLI alternative using `codex mcp add`
- [ ] Note how to verify active servers in Codex
