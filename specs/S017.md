# S017: Allow worktree via run_git

## Title
Allow worktree via run_git

## Spec Summary
Expand `run_git` to allow the `worktree` subcommand, enabling all worktree operations through the generic git tool.

## Spec Goal
Minimize new tool surface area while exposing git worktree functionality.

## DoD checklist
- [x] Add `worktree` to the allowlist in `src/git_control/tools.py`
- [x] Document `run_git` usage for worktree in `docs/git-control.md` and `docs/git-control-test.md`
- [x] Note that `run_git` does not enforce absolute path arguments
- [x] Add tests for allowlisting and error messaging if needed
- [x] Update `specs/plan.md`
